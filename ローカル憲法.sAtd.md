

---

# **ロカールワークスペース憲法.sAtd.md（IZAKAYA verse・ローカル憲法）**

  

**版**: v1.0

**場所**: リポジトリ直下（/憲法.sAtd.md）

**目的**: 70%到達後の事故（誤解による一括書換・配置ズレ・命名崩壊）を**制度で防止**し、どのAI/人でも**安全に引き継げる**最低限の規範を定める。

2025/09/30現在のケースを例にしているが、基本的にユーザーはこの先もこのルールを厳守してもらいたい

---

## **0. 用語**

- **主任AI**: 既存設計・依存関係を最も把握している主要エージェント。
    
- **新顔AI**: 初参加のエージェント（モデル/拡張/セッション含む）。
    
- **ACK**: レビュー承認トークン（ACK: <commit-hash> の記載）。
    
- **保護領域**: 変更前に必ず承認が必要なディレクトリ群。
    

---

## **1. 最重要ルール（非交渉）**

1. **初回は読解のみ**：新顔AIは**一切書換禁止**。まず**読解レポート**を提出。
    
2. **UNDERSTAND→ACK→編集**：
    
    - runs/ai/UNDERSTAND.md に所定の情報（§2）を提出。
        
    - レビュアが ACK: <commit-hash> を同ファイルへ追記後、初めて変更可。
        
    
3. **1変更=1PR**：小さく、根拠パスを明記（§5）。
    
4. **原本は物理ロック**：docs/SESSION_NOTES.md と schemas/ の仕様原本は保護。
    
5. **地図に従う**：所在・命名は docs/IZAKAYA_MASTER_MAP.md に従う（§3）。
    

---

## **2. 新顔AIの初回タスク（UNDERSTAND提出）**

  

runs/ai/UNDERSTAND.md に以下を**Markdown**で出力せよ（書換は禁止）：

- **フォルダ役割（各3行以内）**：apps/, components/, schemas/, cards/, docs/, scripts/, .github/
    
- **依存主要5鎖**：ファイル→ファイルの依存を5本（最重要と思う順）
    
- **居酒屋コンポーネント 16種の所在**：正式名→相対パス
    
- **変更候補**（まだ適用しない）：目的・影響範囲・根拠パス
    
- **参照元の列挙**：読んだファイル/行範囲（できる範囲で）
    

  

> ここまで出したら**待機**。ACKが付くまで編集はしてはならない。

---

## **3. 地図（MASTER MAP）**

- **所在の真実は常にここ**：docs/IZAKAYA_MASTER_MAP.md
    
- 最低項目：
    
    - **コンポーネント16種**（正式名→相対パス）
        
        - 例）IzakayaMenu v2 → components/menu-v2/…
            
        - 〈TODO: 16種を列挙〉
            
        
    - **ディレクトリ役割**：各3行以内
        
    - **セッションノートの置き場所**：docs/SESSION_NOTES.md 固定（他に書くな）
        
    

---

## **4. 保護領域（レビュー必須）**

- docs/（特に SESSION_NOTES.md, IZAKAYA_MASTER_MAP.md）
    
- schemas/（*.sAtd, *.json の原本）
    
- cards/（V2/V3カード定義）
    
- apps/, components/（実装と再利用部品）
    
- .github/（CI/CD・ルール）
    

---

## **5. PR の必須項目（テンプレ）**

- **何を変えたか（3行）**
    
- **どう確認したか（コマンド/URL）**
    
- **根拠ファイル（パス列挙）**
    
    - 例）docs/AGENTS.md, tasks/QR-001.md, schemas/V2_foundation_master_unified.sAtd.json
        
    

  

> 根拠パスの無いPRは**受理しない**。

---

## **6. 命名・配置（最低限）**

- **新規フォルダ**を作る時は必ず docs/IZAKAYA_MASTER_MAP.md に**先に宣言**。
    
- **コンポーネント名**は「英語正式名＋世代」（例：IzakayaMenu v2）
    
    - ディレクトリは kebab-case、ファイルは lowerCamelCase.ts(x) を基本。
        
    
- **セッションノート**の追記先は**固定**：docs/SESSION_NOTES.md。他へ書かない。
    

---

## **7. バックアップ規定（地図付き）**

- **全体バックアップ**：地図とノートを同梱して固める。
    

```
tar -czf izakaya_backup_$(date +%Y%m%d).tar.gz \
  docs apps components cards schemas scripts .github \
  runs/ai/UNDERSTAND.md docs/IZAKAYA_MASTER_MAP.md docs/SESSION_NOTES.md
```

-   
    
- **タグ**：大きな変更前後で git tag backup-YYYYMMDD → git push --tags
    

---

## **8. 物理ロック（誤上書き防止・macOS）**

```
# 解除: chflags nouchg <path>
chflags uchg docs/SESSION_NOTES.md
chflags uchg schemas/*.sAtd
```

---

## **9. 実装保護（Gitフック/レビュー）**

  

### **9.1 CODEOWNERS（レビュー必須）**

  

.github/CODEOWNERS（この内容で配置すること）

```
/docs/*           @motookoyama
/apps/**          @motookoyama
/components/**    @motookoyama
/cards/**         @motookoyama
/schemas/**       @motookoyama
```

### **9.2 PRテンプレ**

  

.github/PULL_REQUEST_TEMPLATE.md

```
## 何を変えたか（3行）
-
## どう確認したか（コマンド/URL）
-
## 根拠ファイル（パス列挙・必須）
-
```

### **9.3 pre-commit（ACKなし変更ブロック）**

  

.git/hooks/pre-commit に以下を保存（chmod +x）

```
#!/usr/bin/env bash
set -e
PROTECT=(docs/ apps/ components/ cards/ schemas/)
ACK_FILE="runs/ai/UNDERSTAND.md"
ACK_OK=$(grep -c "^ACK:" "$ACK_FILE" 2>/dev/null || true)
CHANGED=$(git diff --cached --name-only)
NEED_BLOCK=0
for f in $CHANGED; do
  for p in "${PROTECT[@]}"; do
    [[ "$f" == $p* ]] && NEED_BLOCK=1
  done
done
if [[ $NEED_BLOCK -eq 1 && $ACK_OK -eq 0 ]]; then
  echo "✋ 承認(ACK)なしで保護領域を変更しようとしています。"
  echo "まず runs/ai/UNDERSTAND.md を提出し、レビュー後に ACK を付与してください。"
  exit 1
fi
```

---

## **10. 作業フロー（強制）**

1. **新顔AI**：§2のUNDERSTAND提出 → **待機**
    
2. **レビュア**：内容確認 → ACK: <commit-hash> を追記
    
3. **実装**：**小PRのみ**。テンプレ（§5）に沿って作成
    
4. **CI**：緑以外はマージ不可
    
5. **staging確認→本番**：人間が承認（自動配備はしない）
    

---

## **11. ロールバック**

- 「直前の安定タグ」へ戻す。gh release view から実行 or git revert PR。
    
- **セッションノート**は**最新を残し**、ロールバック理由だけ追記。
    

---

## **12. 補則（Boost/アダプタの切替）**

- ローカルLLM・Ollamaの**Boost切替**は adapters/*.yaml の boost: true/false で管理。
    
- 速さ優先（設計/要約）= boost:true、精度優先（コード/検証）= boost:false。
    
- 変更時はPRで**根拠パス**（設定ファイル）を明記。
    

---

## **付録A：新顔AIオンボ指示（コピペ用）**

```
まずプロジェクトを読み取りだけで把握してください。書き換えは厳禁です。
次を runs/ai/UNDERSTAND.md にMarkdownで出力：
- フォルダ役割（各3行）
- 依存主要5鎖
- 居酒屋コンポーネント16種の所在（正式名→パス）
- 変更候補（未適用）と影響範囲
- 参照したファイル/行
出力後は停止。レビュアによる ACK 追記まで編集してはいけません。
```

## **付録B：地図(MAP)雛形**

  

docs/IZAKAYA_MASTER_MAP.md

```
# IZAKAYA MASTER MAP
## コンポーネント16種（正式名→パス）
- 〈TODO:16件列挙〉
## ディレクトリ役割
- apps/: …
- components/: …
- schemas/: …
- cards/: …
- docs/: …
- scripts/: …
- .github/: …
## セッションノート
- 追記先は docs/SESSION_NOTES.md 固定（他へ書くな）
```

---

以上。

このファイルを直下に置き、**CODEOWNERS/PRテンプレ/フック/地図**を順に適用すれば、**主任AIでも誤って壊すリスク**を最小化できます。

埋めるべき〈TODO〉があれば、私の方で雛形に追記して返します。